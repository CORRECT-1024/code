//ans = zong - ((lenlr为奇数时)ai 和 sumlr 同奇偶) - (ai为中心时，ai左边和ai右边相同sum的次数)
//O(n^2log) ? 
//困了，明天再写
#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
typedef unsigned long long ULL;

void work() {
    
}
int main()
{
    #ifdef QHK
    freopen("qi.in","r",stdin);
    freopen("qi.out","w",stdout);
    #endif
    ios::sync_with_stdio(false); cin.tie(0); 
    int T=1;
    // cin >> T;
    while(T--){
        work();
    }

   return 0;
}
